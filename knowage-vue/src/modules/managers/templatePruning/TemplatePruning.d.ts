export interface iFolder {
    id: number,
    name: string,
    biObjects: Array,
    exportable?: boolean
}

export interface iDocument {
    id: number,
    name: string,
    exportable?: boolean
}