<div style='width:650px;font-size:10px;font-family:verdana;' >
<br/>
Per inserire un attributo di profilo è necessario utilizzare la sintassi  <b>${nome_attributo_profilo}</b>. 
<br/>
<br/>
Per inserire un attibuto di profilo multivalore è necessario utilizzare la sintassi
<b>${name_profile_attribute(prefix;splitter;suffix)}</b>. 
<br/>
L'attributo di profilo verrà sostituito da prefix+(lista di valori separati dallo splitter)+suffix 
<br/>
<b>Esempio:</b> ${nome_attributo_profilo( in (';',';') )}
<br/>
</div>
