<div style='width:650px;font-size:10px;font-family:verdana;' >
<br/>
Pour ins&eacute;rer un attribut simple de profil dans une liste de valeur fixe utilisez <b>${name_profile_attribute}</b>.
<br/>
<br/>
Pour ins&eacute;rer un attribut multiple de profil dans une liste de valeur fixe utilisez : 
<b>${name_profile_attribute(prefix;splitter;suffix)}</b>. 
<br/>
L'attribut de profil sera remplac&eacute; par prefix+(Liste de valeurs s&eacute;par&eacute;es par les s&eacute;parateurs)+suffix
<br/>
<b>Exemple:</b> ${name_profile_attribute( in (';',';') )}
<br/>
</div>
